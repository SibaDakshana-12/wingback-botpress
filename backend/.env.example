# Botpress webhook ID is supplied per-resource via the onboarding API,
# not via environment variables. No secrets are required to run the
# backend itself.

# Optional: restrict CORS to specific origins (comma-separated).
# Defaults to "*" if unset.
ALLOWED_ORIGINS=*

# Optional: override the SQLite database file path.
# DATABASE_PATH=botpress_connector.db